@page
@model LogoutModel
@{
    ViewData["Title"] = "Deconectați-vă";
}

<header>
    <h1>@ViewData["Title"]</h1>
    @{
        if (User.Identity?.IsAuthenticated ?? false)
        {
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Page("/", new { area = "" })" method="post">
                <button type="submit" class="nav-link btn btn-link text-dark">Faceți clic aici pentru a deconecta</button>
            </form>
        }
        else
        {
            <p>V-ați deconectat cu succes din aplicație.</p>
        }
    }
</header>
